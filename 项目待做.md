# 项目待做

更新时间：2026-09-10  
本文件区分“当前已经确认实施”与“明确暂缓”。它不是把所有技术想法都当作本轮必须做的事项。

## 当前已完成

以下两项已在 WCS 后端完成：

| 项目 | 完成内容 |
| --- | --- |
| 任务状态机 | 增加 `TaskLifecycleService`，统一下发、接收、阶段回调、失败和未知结果的合法状态转换；重复或乱序回调只保留流水，不重复执行业务副作用。 |
| 任务完成协调 | 增加 `TaskCompletionCoordinator`，集中处理 `LOAD_FINISH` 库存推进、自动任务的锁/库存释放，以及非自动任务的终点模块队列。 |

自动下发目前仅在 HTTP 成功且 GRCS 响应 `success=true` 时被视为接收成功。网络异常不会自动重试，状态会保留为 `Unknown`。

## 当前没有排期的实施项

用户已明确本轮不继续以下工作。代码和设计文档应保持为当前状态，后续有实际需求时再单独立项。

| 暂缓项 | 暂缓范围 | 重新启动时需要先确定的事 |
| --- | --- | --- |
| RCS 实现 | RCS 协议、调度、RCS 控制台、实时推送与数据模型 | 要替代的 GRCS 接口清单、调度规则、迁移策略 |
| 自动化测试 | 单元、集成、端到端测试工程 | 优先覆盖的主流程和可用的隔离数据库/GRCS 模拟环境 |
| Mock 重构 | Mock 卡、模拟回调、审批流的重新设计 | Mock 在正式环境和演示环境中的职责边界 |
| 接口库存盘点 | 统一梳理/冻结 WCS API 清单 | 外部调用方、版本策略、废弃接口处理 |
| 任务持久化恢复 | reservation、队列、自动化循环在重启后的恢复 | 重启语义、是否允许恢复下发、人工接管界面 |
| GRCS 自动重试 | 下发、信号或查询的自动重试 | 幂等键、GRCS 去重契约、最大次数、退避与告警 |
| 认证与权限 | 登录、角色、接口授权、操作审计 | 用户体系、角色矩阵和部署网络边界 |
| 前端架构调整 | 大规模页面/状态管理重构 | 具体痛点、交互原型和兼容范围 |
| Contracts/Shared 重构 | DTO/实体进一步拆分、共享设施边界调整 | 兼容策略和迁移成本 |

## 建议在下一轮优先评估的事项

这些不是当前待实施承诺；当系统进入联调或上线准备阶段时，建议按以下顺序重新评估。

### 1. 受控环境下的手工任务闭环验证

目的：确认新增的状态机与完成协调器和真实 GRCS 回调契约一致。

建议验证场景：

1. 自动任务正常经过 `CREATED → START → LOAD_FINISH → FINISHED`，检查源库存、目标库存和站点锁最终状态。
2. 同一 `LOAD_FINISH` 或 `FINISHED` 回调重复发送，确认流水允许追踪但库存和锁只处理一次。
3. 先收到 `FINISHED` 或收到乱序回调，确认不会把已完成任务倒退。
4. GRCS 返回 HTTP 成功但 `success=false`，确认不会写成功台账或激活资源占用。
5. GRCS 超时/断网，确认任务为 `Unknown`，不会生成重复下发。
6. 自动任务下发前或运行中强制结束，确认本进程登记的站点锁被释放。

该项是手工联调检查，不等同于新增自动化测试工程。

### 2. 重启恢复与不确定下发的处理

当前的 `task_records` 和库存账本可持久化，但下列内容仍是内存状态：任务状态机的实时状态、完成协调队列、reservation、自动化闸门和运行中的自动模板循环。服务在这些状态存在时重启，会丢失当前进程的收尾上下文。

若以后实施，应先定义每种状态的运营语义：

| 场景 | 需要的决定 |
| --- | --- |
| 服务在 GRCS 调用超时后重启 | 是否按任务号反查 GRCS，再决定接收、失败或人工介入 |
| 服务在 `LOAD_FINISH` 后重启 | 如何从台账和库存账本重建运输中任务 |
| 服务在 `FINISHED` 后、释放资源前重启 | 如何确保锁释放和目标库存写入只发生一次 |
| 自动模板运行期间重启 | 默认停止、自动恢复，还是显示为待人工确认 |

这项必须和 GRCS 的任务查询及幂等能力一起设计，不能仅靠本地重试实现。

### 3. 生产部署保障

正式部署前需要根据实际网络环境完成：

- 设置 `CORS_ORIGIN` 为明确的前端域名。
- 为控制台和管理 API 加入认证、授权及关键操作审计。
- 设置 `WCS.db` 的备份、保留、恢复和容量监控策略；目前 `task_records` 只保留最近 10,000 条。
- 明确 SQLite 是否仍满足部署时的并发和高可用要求；若部署为多实例，需要替换进程内锁、闸门和队列。
- 记录 GRCS 地址、超时、失败日志和健康检查的运维告警方式。

## 恢复实施时的验收标准

| 未来工作 | 最小验收标准 |
| --- | --- |
| 重启恢复 | 重启后能识别在途/未知任务，不重复释放库存或重复下发，并给出可操作的人工处理入口。 |
| 自动重试 | 仅对具有幂等保障的调用重试；每次尝试、最终结果和人工接管原因可追踪。 |
| 测试 | 使用隔离数据库和可控 GRCS 替身，覆盖正常完成、重复回调、乱序回调、失败和重启边界。 |
| RCS | 在不改变 WCS 调用方式的前提下，实现已确认的 GRCS 协议子集，并有端到端联调证据。 |
| 权限 | 未登录或无权限用户不能读取或操作受保护的 WCS 管理功能，关键动作可审计。 |
