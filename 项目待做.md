# 项目待做

更新时间：2026-09-18
本文件只记录根据当前代码仍需实施或重点验证的事项。

## 1. 最近已完成

| 项目 | 当前结果 |
| --- | --- |
| WCS 后端目录整理 | 自动化、控制台和基础设施已按 Inventory、Modules、Tasks、Templates、Stores 等职责分目录。 |
| 地图页面拆分 | 地图读取、系统设置、地图创建均有独立 Razor 子组件和 `.razor.cs`。 |
| 统一实时连接 | SignalR 统一为 `/hubs/wcs-realtime`，任务、自动化、日志、信号、移动任务、归巢和配置通知共用一个连接。 |
| 减少 HTTP 轮询 | 自动化状态、自动化日志和信号确认改为 SignalR；WCS/GRCS 健康检查降为约 5 秒一次。 |
| 多标签页同步 | 地图、范围、设置、任务模板和功能模块修改后发送配置变更通知；WCS 地址通过 `storage` 事件同步并触发重连。 |
| 动态库存选取 | 每个选库存步骤重新读取 WCS 当前账本，不再使用整轮固定快照。 |
| 并发资源保护 | `TryPick` 原子领取托盘/货物，起点和终点使用任务锁，允许轮次重叠时避免重复占用。 |
| WCS 完成阶段 | 自动化由等待 `FINISHED` 改为等待 `WCS_COMPLETED`，库存、模块或回库失败会终止后续步骤。 |
| 分拣回库闭环 | 回库任务在模块 HTTP 前预登记并锁定目标；回库完成后父分拣任务才完成。 |
| 分拣台关联 | 库存地图支持人工分拣台关联多个实际分拣台，分拣完成时按实际空位落库。 |
| 模块执行日志 | `module_exec_logs` 持久化模块请求、响应和执行阶段，并通过 SignalR 增量推送。 |
| 可视化地图编辑 | 已有无限画布、缩放、框选、点阵、X/Y 对齐、多选属性和多种站点图标。 |

## 2. 建议优先实施

### P0：地图编辑保存

当前“保存到库存地图”按钮只是提示，没有持久化。建议保存时：

1. 将编辑器中的站点转换为 `MapStationLite`。
2. 校验 Mark 唯一、坐标有效、站点类型有效，禁止覆盖有在途任务的地图。
3. 复用 `POST /api/wcs/map/upload` 保存到 `kv.map_stations`。
4. 调用现有 `SyncMapSlots()` 创建或更新 `wcs_slots`；新站点库存为空、状态按站点类型计算。
5. 明确删除旧站点规则：有库存、任务锁或分拣关联的站点不能直接删除。
6. 保存成功后广播 `ConfigurationChanged: map`，其他标签页重新加载。

最小验收：刷新页面和重启 WCS 后地图仍存在；库存数据不因只修改坐标而丢失。

### P0：任务异常恢复入口

当前任务收尾能从数据库恢复一部分，但下发结果未知、长期等待回库、库存收尾失败仍需要人工处理。建议增加异常任务面板：

- 显示 `DISPATCH_UNKNOWN`、`WCS_WAITING_RETURN`、`WCS_FINALIZATION_FAILED` 及原因；
- 支持按任务号向 RCS 查询实际状态；
- 支持经确认后重试收尾、标记失败、解除锁或执行库存同步；
- 每次人工处理写入审计记录。

### P1：库存实时推送

当前库存地图需要进入页面或手动刷新。后续可以在 RCS 提供 SignalR 后：

- RCS 库存/位置变化推给 WCS；
- WCS 更新 `wcs_slots` 后通过现有 `/hubs/wcs-realtime` 推送 `InventoryChanged`；
- 前端收到通知后拉取库存增量或最新快照；
- 在途位置采用限频更新，避免地图高频重绘。

### P1：自动化重启恢复

需要明确重启策略并实现：

- 是否自动恢复轮询和选中的模板；
- 如何恢复正在等待 `WCS_COMPLETED` 的轮次；
- 如何从 RCS 对账已下发但未完成的任务；
- 如何避免重启后重复下发和重复释放库存。

建议先保存运行配置和轮次 ID，再以 `task_records` + RCS 状态对账恢复，不直接重放模板步骤。

### P1：自动化测试

建立隔离 SQLite 和可控 RCS 替身，优先覆盖：

- 原子领取同一库存的并发竞争；
- 重叠轮次不会重复选择托盘、货物或终点；
- 重复、乱序 `START/LOAD_FINISH/FINISHED`；
- 分拣任务、回库预登记、回库成功和回库失败；
- 模块 HTTP 明确失败与结果未知；
- 服务在 `FINISHED` 后、WCS 收尾前重启。

### P2：上线基础能力

- 登录和角色权限；
- 关键操作审计；
- 限定生产 CORS 域名；
- 数据库备份、归档和恢复演练；
- 连接中断、任务卡住、库存不一致告警。

## 3. 需要重点联调验证

1. **普通搬运闭环**：`CREATED → DISPATCHED → START → LOAD_FINISH → TRANSIT → FINISHED → WCS_COMPLETED`，确认起点清空、终点落位和锁释放。
2. **重叠轮次**：把轮询间隔设得短于任务耗时，确认不同轮次不会领取相同库存或锁定相同储位。
3. **动态选库存**：同一自动化模板前一步改变库存后，后一步应使用变化后的最新账本，而不是启动时快照。
4. **分拣落位**：人工分拣台关联多个实际分拣台后，确认 RCS 实际站点优先、空位选择正确，任务看板终点同步更新。
5. **分拣回库**：确认回库任务在模块调用前已有完整台账，RCS `START` 生成在途，回库 `WCS_COMPLETED` 后父分拣任务立即完成。
6. **回库 HTTP 失败**：明确 HTTP 失败释放目标锁；超时或网络异常保留锁并显示 `DISPATCH_UNKNOWN`。
7. **WCS 收尾失败**：模拟无在途记录、分拣台无空位、终点模块失败，确认自动化不继续下发后续步骤，并显示中文原因。
8. **SignalR 重连**：修改 WCS 地址、多标签页保存配置、重启后端，确认只建立新地址连接且新标签页收到完整快照。
9. **配置跨标签页**：修改范围、地图、任务模板和功能模块，确认其他标签页收到通知并刷新权威数据。
10. **服务重启收尾**：任务已有 `FINISHED` 但没有最终阶段时重启，确认后端能够补做库存和模块收尾。

## 4. 暂未实施

| 事项 | 当前边界 |
| --- | --- |
| 地图编辑器保存 | 只有 UI 和内存数据，按钮未接后端。 |
| 库存增删按钮 | 仍是界面占位；纯货物手工入库和 RCS 托盘批量生成已可用。 |
| 指定储位生成托盘 | RCS `/AutoContainerEnter` 只能按楼层批量填充，不能指定框选储位。 |
| GRCS 自动重试 | 缺少统一幂等键、反查和人工接管规则。 |
| 完整自动化恢复 | 任务数据持久化，但运行轮次和等待器没有完整恢复。 |
| RCS 自研业务 | `RcsBackend` 仍为骨架。 |
| 权限和审计 | 管理接口尚无生产级鉴权和操作审计。 |

## 5. 代码质量整理

1. 修复 `WcsModuleExtensions.cs` 等早期文件中的乱码注释。
2. 删除仍描述旧固定库存快照、旧 Hub 地址或旧内存映射的过期注释。
3. 统一 `TaskStageRealtimeHub` 的类名与实际“WCS 实时 Hub”职责；当前路由已经改名，但 C# 类型名仍保留旧名称。
4. 为任务阶段字符串建立集中常量，减少 `WCS_COMPLETED`、`WCS_WAITING_RETURN` 等字符串散落。
5. 明确 `Stop`、`ForceEnd` 和异常任务人工解除锁的产品语义，并让界面提示与后端行为保持一致。
