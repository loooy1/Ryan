# 项目待做

更新时间：2026-09-13
本文件记录当前尚未实施的事项。已提交的 WCS 任务记录、模块阶段、储位锁定和在途合并逻辑不在本待办范围内。

## 已完成的本轮改造

| 项目 | 已实现内容 |
| --- | --- |
| 任务阶段统一记录 | `task_records` 记录创建、下发、模块、GRCS 回调、在途、取消和信号事件。 |
| 旧表收敛 | 已通过迁移删除 `workflow_state` 和 `module_exec_logs`；相关展示由 `task_records` 派生。 |
| 模块执行顺序 | 起点前模块失败阻止下发；下发后模块和终点后模块均记录执行结果。 |
| 库存与储位锁 | 使用 `wcs_slots.selection_status` 与 `task_lock_id` 控制储位起点、终点选择。 |
| 在途展示 | 一条 `TRANSIT:托盘号(货号)` 同时保存托盘和货物，并在库存页显示为一条在途数据。 |
| 任务记录字段收敛 | `task_records` 已用起点、终点字段替代旧路线字段，并以 `stage_status` 的 `success`、`fail`、`callback` 替代 `ok`。 |
| 模块库存副作用 | 模块可配置货物到达或货物移除；模块成功后按其执行位置更新当前任务货物，并记录 `INVENTORY_EFFECT:*`。 |
| 库存地图与同步 | 库存页已有常驻地图、悬停库存详情、框选储位、RCS 货物模型读取，以及“RCS 同步至 WCS”账本重建。 |
| 手工库存录入 | 纯货物可指定储位写入 WCS 后逐条同步 RCS；纯托盘通过 RCS 按楼层批量生成后回写 WCS。 |

## 当前不实施

| 事项 | 暂缓原因或前置条件 |
| --- | --- |
| RCS 业务实现 | 需要先明确将替代的 GRCS 接口、调度规则、车辆模型和迁移范围。 |
| GRCS 自动重试 | 需要幂等键、GRCS 去重能力、最大次数、退避策略和人工接管规则。 |
| 自动化测试工程 | 需要隔离数据库、可控 GRCS 替身和优先验证的业务场景。 |
| Mock 与审批流重构 | 需要先明确演示环境和正式环境中 Mock 的职责边界。 |
| Contracts/Shared 大规模拆分 | 当前没有明确兼容目标，暂不进行结构性重构。 |
| 前端状态管理重构 | 先收集具体页面的性能或交互问题后再定范围。 |
| 库存地图的真实增删操作 | 当前增加/删除托盘、货物按钮只展示规则，尚未接入 WCS 写库或 RCS 调用；需先确定每种操作的 RCS 同步策略、失败补偿和审计方式。 |
| 托盘指定储位生成 | 当前 RCS `/AutoContainerEnter` 只支持按楼层批量填充空闲 RACK，不能把托盘生成到框选的指定储位；需由后续 RCS 接口提供指定落位能力。 |

## 建议优先验证

这些是联调检查项，不表示立即修改代码。

1. **正常搬运闭环**：验证 `CREATED → DISPATCHED → START → LOAD_FINISH → TRANSIT → FINISHED`，确认起点清空、在途单行显示、终点落位和站点锁释放。
2. **带货托搬运**：确认托盘和货物在 `task_records` 中只有一条在途记录，`ContainerCode` 和 `CargoCode` 均正确。
3. **起点前模块失败**：确认任务不下发，记录 `BEFORE_MODULE:*` 和 `CANCELLED`，库存和储位锁恢复可用。
4. **重复或乱序回调**：重复发送 `LOAD_FINISH`、`FINISHED`，或先发 `FINISHED`，确认库存与锁不会被重复处理。
5. **终点模块失败或变慢**：确认失败结果记录在 `AFTER_END_MODULE:*`，但已完成任务的库存落位与资源释放不被阻塞。
6. **服务重启边界**：在下发后、`LOAD_FINISH` 后和 `FINISHED` 后分别重启，确认数据库账本符合预期，并通过人工同步处理未恢复的运行期队列。
7. **纯货物手工录入**：框选多个空储位，确认先取得 RCS 货物模型、WCS 写库与逐条 `/api/Cargo/Enter` 均成功；再模拟某一条 RCS 失败，确认批次结果可定位到具体储位。
8. **纯托盘手工录入与同步**：确认 RCS 按当前楼层生成托盘后，执行“RCS 同步至 WCS”能反映实际落位；在存在在途任务时，确认同步被拒绝。
9. **模块库存副作用**：分别在起点前、起点后和终点后配置货物到达/移除模块，确认只影响当前任务货物，托盘不被误删，并产生 `INVENTORY_EFFECT:*` 记录。

## 需要产品规则后再做的事项

| 事项 | 需要先确认的规则 |
| --- | --- |
| 重启恢复 | 重启后是停止任务、从 GRCS 反查任务，还是允许人工确认后续跑。 |
| 不确定下发结果 | HTTP 超时时如何判断 GRCS 是否已接收，及人工处理入口。 |
| 自动化轮次重叠 | 轮询周期小于单轮耗时时，是跳过、排队还是允许并行。 |
| 强制结束 | 已下发给 GRCS 的任务是否继续占用库存/储位，何时必须执行人工同步。 |
| 数据归档 | `task_records` 的保留周期、备份频率、恢复流程和可查询范围。 |
| 上线安全 | 用户角色、权限矩阵、操作审计、CORS 域名和告警方式。 |

## 后续验收标准

| 未来工作 | 最小验收标准 |
| --- | --- |
| 重启恢复 | 不重复下发、不重复释放库存；在途和异常任务有明确的人工处理入口。 |
| 自动重试 | 只重试具备幂等保护的接口；每次尝试和最终结果可追溯。 |
| 自动化测试 | 使用隔离 SQLite 与 GRCS 替身覆盖正常、失败、重复、乱序和重启场景。 |
| RCS 实现 | 在不改变 WCS 调用方式的前提下，实现已确认 GRCS 协议子集并完成端到端联调。 |
| 权限与运维 | 未授权用户无法操作管理接口；关键操作可审计，数据库可备份和恢复。 |
